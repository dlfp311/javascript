const firstnumber = window.prompt("첫번째 숫자는?");
const secondnumber = window.prompt("두번째 숫자는?");
const result = Number(firstnumber) + Number(seconndnumber);
window.alert(`${firstnumber} + ${seconndnumber} = ${result}`);

const age = window.prompt("귀하의 나이는?");
const year = 2026 - Number(age);
window.alert(`귀하의 태어난 년도는 ${year}입니다`);
